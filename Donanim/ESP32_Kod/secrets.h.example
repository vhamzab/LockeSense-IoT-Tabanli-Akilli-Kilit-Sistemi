// ÖRNEK DOSYA: Bu dosyayı secrets.h olarak kopyalayın ve değerleri doldurun.
// secrets.h dosyası .gitignore ile korunur ve GitHub'a yüklenmez.

#ifndef SECRETS_H
#define SECRETS_H

// Wi-Fi Bilgileri
const char* ssid = "YOUR_WIFI_SSID_HERE";
const char* password = "YOUR_WIFI_PASSWORD_HERE";

// MQTT Broker (Sunucu) Bilgileri
const char* mqtt_server = "YOUR_MQTT_SERVER_HERE";
const int mqtt_port = 1883;

// MQTT Konusu (Topic) - Bu isim hem ESP32 hem de PC'de aynı OLMALIDIR
const char* mqtt_topic = "/pc_kilit/status";

#endif

